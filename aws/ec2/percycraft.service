[Unit]
Description=Percycraft cluster
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/local/bin/docker-compose -f /percycraft/docker-compose.mc.yml --env-file /percycraft/.env up
TimeoutSec=1200

[Install]
WantedBy=multi-user.target